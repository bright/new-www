import React from 'react'
import { FramnaLink as Link } from '../../../framna/FramnaLink'
import * as Styled from './tags.styled'

export const TagsWrapper = Styled.TagsWrapper
export const TagsLink = Styled.TagsLink
export const SubTagsWrapper = Styled.SubTagsWrapper
export const TagsSelect = Styled.TagsSelect
